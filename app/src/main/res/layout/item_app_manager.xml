<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:padding="8dp"
    
    android:background="@drawable/home_grid_item_select"
    android:layout_height="wrap_content" >
    <ImageView 
        android:id="@+id/app_item_icon_iv"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@mipmap/ic_launcher"
        />
    <TextView 
        android:id="@+id/app_item_name_tv"
        style="@style/NormalTextStyle"
        android:text="应用名"
        android:singleLine="true"
        android:layout_toRightOf="@+id/app_item_icon_iv"
        android:layout_marginLeft="5dp"
        android:layout_marginTop="3dp"
        />
    <TextView 
        android:id="@+id/app_item_install_tv"
        style="@style/NormalTextStyle"
        android:layout_below="@id/app_item_name_tv"
        android:layout_alignLeft="@+id/app_item_name_tv"
        android:text="手机内存"
        android:textSize="16sp"
        android:layout_marginTop="5dp"
        />
    <TextView 
        android:id="@+id/app_item_size_tv"
        style="@style/NormalTextStyle"
        android:textSize="14sp"
        android:text="大小"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@id/app_item_install_tv"
        android:layout_marginRight="5dp"
        
        />
    

</RelativeLayout>
